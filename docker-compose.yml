version: '3.8'

services:
  database:
    image: ghcr.io/bezakrobin
    container_name: mssql_server
    ports:
      - "1433:1433"
    environment:
      # This is required by the base MSSQL image
      ACCEPT_EULA: "Y"